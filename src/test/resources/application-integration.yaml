spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:8.0.31:///EVA?user=root?password=password
    username: root
    password: password

logging:
  level:
    org.jooq.tools.LoggerListener: DEBUG